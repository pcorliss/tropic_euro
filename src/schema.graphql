type Action {
    key: ID!
}

type GameState {
    id: ID!
    json: String
    actionCounter: Int
    roundCounter: Int
    availableActions(playerId: String): [Action]
}

type Query {
    hello: String
    gameState(id: String): GameState
}